# Keybox Revocation Checker (XML UI)

Android app (XML layouts) that parses a `keybox.xml` file, shows certificate details, checks Google attestation revocation list, and displays a final integrity verdict.

Open in Android Studio, build, run. Includes GitHub Actions workflow for building APKs.
